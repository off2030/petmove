/**
 * 여정 의견 라우트 (/cases/[id]/feedback) — 실제 화면은 TabHost detail 레이어
 * (components/feedback/feedback-screen.tsx). 이 page 는 URL 진입점만 제공.
 */
export default function CaseFeedbackPage() {
  return null
}
