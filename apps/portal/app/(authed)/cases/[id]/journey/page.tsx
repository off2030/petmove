/**
 * 준비(여정) 탭 라우트 — 실제 화면은 (authed) layout 의 TabHost 가 상주 pane
 * (components/journey/journey-tab.tsx) 으로 그린다. 이 page 는 URL 진입점만 제공.
 * 탭 전환은 pushState(tab-nav.ts) 라 이 라우트로의 네비게이션 자체가 드물다.
 */
export default function CaseJourneyPage() {
  return null
}
