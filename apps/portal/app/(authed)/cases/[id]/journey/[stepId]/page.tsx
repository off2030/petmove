/**
 * step 상세 라우트 — 실제 화면은 (authed) layout 의 TabHost 가 detail 레이어
 * (components/journey/step-detail-screen.tsx) 로 그린다. 이 page 는 URL 진입점만 제공.
 */
export default function CaseJourneyStepPage() {
  return null
}
