/**
 * 내 케이스 목록 라우트 (/cases) — 실제 화면은 TabHost detail 레이어
 * (components/cases/cases-hub-screen.tsx). 이 page 는 URL 진입점만 제공.
 * (구 server page 의 listMyCases 재조회·redirect 는 client 셸이 Context 로 대체.)
 */
export default function CasesPage() {
  return null
}
