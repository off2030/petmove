'use client'

import { ServicesView } from '@/components/services/services-view'

/**
 * 서비스 탭 (/services) — bottom-nav 의 '서비스' 탭 도착지.
 * 펫무브 유료 상품 안내(오프라인 방문 올케어 / 온라인 가이드 & 점검).
 * case-외(내 정보처럼 caseId 불필요) 화면.
 */
export default function ServicesPage() {
  return <ServicesView />
}
