<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- 상태바: 라이트 앱 배경(stone)에 맞춰 배경색 + 어두운 아이콘(흰 아이콘이 안 보이던 문제 수정). -->
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Android 12+ 시스템 스플래시: 배경색 미지정 시 검정으로 뜨던 문제 수정. 스플래시 후
             AppTheme.NoActionBar 로 전환(상태바 설정 적용). -->
        <!-- 배경을 앰버로 — 흰 P 가 밝은 배경에 안 보이던 문제 해결(마스터처럼 앰버 바탕+흰 P).
             iconBackgroundColor(원형 배경)는 기기에서 안 그려져 제거하고 전체 배경을 앰버로. -->
        <item name="windowSplashScreenBackground">@color/splash_amber</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>